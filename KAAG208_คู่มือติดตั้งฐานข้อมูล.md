# คู่มือติดตั้งฐานข้อมูลเช็คชื่อ/ความคืบหน้า/Learning Analytics (Google Apps Script + Google Sheet)

ใช้เวลาประมาณ 10 นาที ทำครั้งเดียวจบ ไม่ต้องมีความรู้เขียนโปรแกรมมาก่อน

## สิ่งที่จะได้

- Google Sheet ที่เก็บข้อมูล 2 ชีตอัตโนมัติ:
  - **Log** — ประวัติการเช็คชื่อและการทำแบบฝึกหัดครบของนักศึกษาทุกคน (รหัสนักศึกษา, อีเมล, ชื่อ, ครั้งที่, วันเวลา)
  - **QuizLog** — ประวัติการตอบแบบฝึกหัดทุกข้อ ทุกครั้งที่ลอง (ถูก/ผิด, หัวข้อ, จำนวนครั้งที่พยายาม) ใช้เป็นฐานข้อมูลสำหรับระบบ **Learning Analytics แบบพยากรณ์** ที่คำนวณระดับความพร้อมของนักศึกษาและแนะนำหัวข้อที่ควรทบทวนเป็นรายบุคคล
- ลิงก์ "Web App" 1 ลิงก์ ที่ทั้งเว็บบทเรียนออนไลน์และหน้า Dashboard สำหรับอาจารย์จะใช้เชื่อมต่อเพื่ออ่าน/เขียนข้อมูลนี้แบบเรียลไทม์
- หน้า **Dashboard สำหรับอาจารย์** (ไฟล์แยกต่างหาก `KAAG208_instructor_dashboard.html`) แสดงอันดับความพร้อมของนักศึกษาทุกคน พร้อมหัวข้อที่ควรทบทวน — เข้าถึงได้เฉพาะผู้ที่มีรหัสผ่านอาจารย์เท่านั้น

## ขั้นตอน

1. **สร้าง Google Sheet ใหม่**
   ไปที่ [sheets.google.com](https://sheets.google.com) → สร้างสเปรดชีตเปล่า → ตั้งชื่อ เช่น `KAAG208 Attendance DB`

2. **เปิด Apps Script**
   เมนู `Extensions` (ส่วนขยาย) → `Apps Script` จะเปิดหน้าต่างแก้ไขโค้ดขึ้นมา

3. **วางโค้ด**
   ลบโค้ดเดิมทั้งหมดในไฟล์ `Code.gs` ออก แล้วคัดลอกเนื้อหาทั้งหมดจากไฟล์ `Code.gs` ที่แนบมาพร้อมกันนี้ไปวางแทน แล้วกดบันทึก (ไอคอนรูปแผ่นดิสก์ หรือ Ctrl/Cmd+S)

4. **Deploy เป็น Web App**
   มุมขวาบน กดปุ่ม `Deploy` → `New deployment`
   - กดไอคอนรูปเฟือง (Select type) → เลือก `Web app`
   - Description: พิมพ์อะไรก็ได้ เช่น `KAAG208 backend`
   - Execute as: **Me (บัญชีของคุณ)**
   - Who has access: **Anyone**
   - กด `Deploy`

5. **อนุญาตสิทธิ์ (Authorize)**
   ระบบจะขอให้ล็อกอินและอนุญาตสิทธิ์ — เลือกบัญชี Google ของคุณ → หากขึ้นหน้าจอเตือน "Google hasn't verified this app" (ปกติมาก เพราะเป็นสคริปต์ของคุณเอง ไม่ใช่แอปสาธารณะ) ให้กด `Advanced` → `Go to [ชื่อโปรเจกต์] (unsafe)` → `Allow`

6. **คัดลอกลิงก์ Web App**
   หลัง Deploy สำเร็จ จะมีกล่องแสดง `Web app URL` ซึ่งลงท้ายด้วย `/exec` — คัดลอกลิงก์นี้ไว้

7. **ใส่ลิงก์ในไฟล์เว็บบทเรียนและไฟล์ Dashboard อาจารย์**
   เปิดไฟล์ `KAAG208_บทเรียนออนไลน์.html` ด้วยโปรแกรมแก้ไขข้อความ (เช่น TextEdit, Notepad, VS Code)
   ค้นหาบรรทัด:
   ```
   const APPS_SCRIPT_URL = "";
   ```
   แล้วแก้เป็น (วางลิงก์ที่คัดลอกมาแทนที่ระหว่างเครื่องหมายคำพูด):
   ```
   const APPS_SCRIPT_URL = "https://script.google.com/macros/s/xxxxxxxxxxxxx/exec";
   ```
   บันทึกไฟล์ แล้วทำแบบเดียวกันในไฟล์ `KAAG208_instructor_dashboard.html` (วางลิงก์เดียวกัน)

8. **ตั้งรหัสผ่านอาจารย์ (สำหรับ Dashboard)**
   ในไฟล์ `Code.gs` ที่วางไว้ใน Apps Script (ขั้นตอนที่ 3) ค้นหาบรรทัด:
   ```
   const INSTRUCTOR_KEY = "kaag208-teacher-2026";
   ```
   แก้ข้อความในเครื่องหมายคำพูดเป็นรหัสผ่านของคุณเอง (ตัวอย่างเช่นรหัสที่จำง่ายแต่คนอื่นเดาไม่ได้) แล้วกดบันทึก จากนั้น **ต้อง Deploy ใหม่** (ดูหมายเหตุข้อ "ถ้าแก้โค้ด Code.gs" ด้านล่าง) รหัสผ่านนี้ใช้กรอกในหน้า Dashboard เพื่อดูข้อมูลนักศึกษาทุกคน — เก็บไว้เป็นความลับ อย่าใส่ในไฟล์ html ของนักศึกษา

9. **ทดสอบ**
   เปิดไฟล์ html อีกครั้ง (ดับเบิลคลิก หรือเปิดผ่านเบราว์เซอร์) — ที่หัวเว็บบทเรียนจะขึ้นข้อความ
   `🟢 เชื่อมต่อฐานข้อมูลแล้ว` แปลว่าเชื่อมต่อสำเร็จ
   ถ้าขึ้น `🔴 เชื่อมต่อฐานข้อมูลไม่สำเร็จ` ให้ตรวจสอบว่าคัดลอกลิงก์ถูกต้อง และตั้งค่า "Who has access" เป็น Anyone แล้ว
   สำหรับ Dashboard อาจารย์ เปิดไฟล์ `KAAG208_instructor_dashboard.html` แล้วกรอกรหัสผ่านอาจารย์ที่ตั้งไว้ในขั้นตอนที่ 8 — ถ้าขึ้นภาพรวมนักศึกษาแปลว่าเชื่อมต่อสำเร็จ

## การดูข้อมูลที่บันทึกไว้

เปิด Google Sheet ที่สร้างไว้ในขั้นตอนที่ 1 จะเห็น 2 ชีต:
- **Log** — มีแถวข้อมูลเพิ่มขึ้นเรื่อย ๆ ทุกครั้งที่นักศึกษาเช็คชื่อหรือทำแบบฝึกหัดครบ
- **QuizLog** — มีแถวข้อมูลเพิ่มขึ้นทุกครั้งที่นักศึกษากด "ตรวจคำตอบ" (ทั้งถูกและผิด) พร้อมหัวข้อและจำนวนครั้งที่พยายาม

สามารถใช้ Pivot Table หรือ Filter ของ Google Sheets กรองดูตามรหัสนักศึกษา/ครั้งที่/วันเวลาได้ตามปกติ หรือดูสรุปแบบพร้อมใช้งานผ่านหน้า **Dashboard สำหรับอาจารย์** แทนก็ได้ (ไม่ต้องเปิด Sheet เอง)

## Learning Analytics แบบพยากรณ์ (สำหรับ นักศึกษา และ อาจารย์)

ระบบคำนวณ **ดัชนีความพร้อม (risk score, 0–100)** ของนักศึกษาแต่ละคนจากพฤติกรรมการทำแบบฝึกหัดจริง 3 ตัวแปร ได้แก่ ความแม่นยำในการตอบ "ครั้งแรก" ต่อข้อ, จำนวนครั้งที่ลองซ้ำเฉลี่ยต่อข้อ, และสัดส่วนครั้งเรียน (1–3) ที่ทำครบแล้ว — เป็นแนวทางเดียวกับระบบ early-alert ที่ใช้ในมหาวิทยาลัยต่างประเทศ (เช่น Purdue Course Signals) พร้อมคำแนะนำหัวข้อที่ควรทบทวนเป็นรายบุคคล (คำนวณจากหัวข้อที่ตอบผิดในความพยายามครั้งแรกบ่อยที่สุด)

- **นักศึกษา** ดูผลวิเคราะห์ของตัวเองได้ที่แท็บ "📊 ผลวิเคราะห์การเรียนของฉัน" ในเว็บบทเรียน (กรอกรหัสนักศึกษาของตัวเอง)
- **อาจารย์** ดูภาพรวมทั้งชั้นได้ที่ไฟล์ `KAAG208_instructor_dashboard.html` (ต้องกรอกรหัสผ่านอาจารย์) — เห็นอันดับความพร้อมของนักศึกษาทุกคน, หัวข้อที่ทั้งชั้นตอบผิดบ่อยที่สุด, และดาวน์โหลดเป็นตารางได้

สูตรคำนวณและน้ำหนักที่ใช้ดูได้ในคอมเมนต์ของฟังก์ชัน `computeInsights_` ในไฟล์ `Code.gs` — ปรับน้ำหนักหรือเกณฑ์ได้ตามดุลยพินิจของผู้สอน

## หมายเหตุสำคัญ

- **ถ้าแก้โค้ด Code.gs ในภายหลัง** ต้อง Deploy ใหม่ทุกครั้งผ่าน `Deploy` → `Manage deployments` → กดไอคอนดินสอ (Edit) ที่ deployment เดิม → เปลี่ยน Version เป็น `New version` → `Deploy` (ลิงก์ `/exec` เดิมจะยังใช้ได้ ไม่ต้องแก้ในไฟล์ html อีก)
- ข้อมูลรหัสนักศึกษา อีเมล และผลการทำแบบฝึกหัดถือเป็นข้อมูลส่วนบุคคล ควรตั้งสิทธิ์การเข้าถึง Google Sheet นี้ให้เฉพาะอาจารย์ผู้สอนเท่านั้น (อย่าแชร์ลิงก์ Sheet แบบสาธารณะ) และ **อย่าแชร์รหัสผ่านอาจารย์หรือลิงก์ไฟล์ Dashboard ให้บุคคลอื่นนอกเหนือจากผู้สอน** ระบบนี้ออกแบบมาเพื่อการตรวจสอบการเข้าเรียน/ความคืบหน้าในการทบทวนเท่านั้น ไม่ใช่ระบบสอบที่มีการป้องกันการโกงขั้นสูง
- ถ้ายังไม่ได้ตั้งค่าลิงก์ (`APPS_SCRIPT_URL` ว่างอยู่) เว็บบทเรียนจะยังใช้งานได้ปกติในโหมดออฟไลน์ แต่จะเก็บข้อมูลเช็คชื่อ/ความคืบหน้าไว้ในเบราว์เซอร์เครื่องนั้นชั่วคราวเท่านั้น ไม่สามารถตรวจสอบข้ามอุปกรณ์ได้ แท็บ "แบบทดสอบจริง" และ "ผลวิเคราะห์การเรียนของฉัน" จะใช้งานไม่ได้จนกว่าจะเชื่อมต่อฐานข้อมูล และหน้า Dashboard อาจารย์จะใช้งานไม่ได้เลยจนกว่าจะตั้งค่าลิงก์เช่นกัน
