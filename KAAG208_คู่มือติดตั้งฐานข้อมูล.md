# คู่มือติดตั้งฐานข้อมูลเช็คชื่อ/ความคืบหน้า/Learning Analytics (Cloudflare Worker + D1)

ใช้เวลาประมาณ 10 นาที ทำครั้งเดียวจบ ไม่ต้องมีความรู้เขียนโปรแกรมมาก่อน

> **หมายเหตุ:** เดิมระบบนี้ใช้ Google Sheets + Apps Script แต่พบว่า Apps Script ส่งข้อมูลกลับมาไม่เสถียร (ล่มต่อเนื่องได้เป็นชั่วโมง) จึงย้ายฐานข้อมูลทั้งหมดมาไว้ใน **Cloudflare D1** (ฐานข้อมูลของ Cloudflare เอง ที่ผูกกับ Worker `kaag208-proxy` ที่สร้างไว้แล้ว) แทน — ไม่ต้องใช้ Google Sheet/Apps Script อีกต่อไป

## สิ่งที่จะได้

- ฐานข้อมูล Cloudflare D1 เก็บข้อมูล 3 ตารางอัตโนมัติ:
  - **log** — ประวัติการเช็คชื่อและการทำแบบฝึกหัดครบของนักศึกษาทุกคน (รหัสนักศึกษา, อีเมล, ชื่อ, ครั้งที่, วันเวลา)
  - **prepost** — คะแนนแบบทดสอบก่อนเรียน/หลังเรียนของแต่ละครั้ง
  - **quiz_log** — ประวัติการตอบแบบฝึกหัดทุกข้อ ทุกครั้งที่ลอง (ถูก/ผิด, หัวข้อ, จำนวนครั้งที่พยายาม) ใช้เป็นฐานข้อมูลสำหรับระบบ **Learning Analytics แบบพยากรณ์** ที่คำนวณระดับความพร้อมของนักศึกษาและแนะนำหัวข้อที่ควรทบทวนเป็นรายบุคคล
- Worker เดิม (`kaag208-proxy.netiya-karaket.workers.dev`) ที่ทั้งเว็บบทเรียนออนไลน์และหน้า Dashboard สำหรับอาจารย์ใช้เชื่อมต่ออยู่แล้ว — **ไม่ต้องแก้ลิงก์ในไฟล์ html ใดๆ เลย** เพราะลิงก์เดิมใช้ได้ต่อ
- หน้า **Dashboard สำหรับอาจารย์** (ไฟล์แยกต่างหาก `KAAG208_instructor_dashboard.html`) แสดงอันดับความพร้อมของนักศึกษาทุกคน พร้อมหัวข้อที่ควรทบทวน และหน้า **ข้อมูลดิบ** ให้ดู/ดาวน์โหลดข้อมูลทุกตารางเป็น CSV — เข้าถึงได้เฉพาะผู้ที่มีรหัสผ่านอาจารย์เท่านั้น

## ขั้นตอน

1. **สร้างฐานข้อมูล D1**
   เข้า [dash.cloudflare.com](https://dash.cloudflare.com) → เมนูซ้าย **Storage & Databases** → **D1 SQL Database** → **Create Database** → ตั้งชื่อ เช่น `kaag208-db` → **Create**

2. **ผูกฐานข้อมูลเข้ากับ Worker**
   เมนูซ้าย **Workers & Pages** → กด Worker ชื่อ `kaag208-proxy` → แท็บ **Bindings** → **Add binding** → เลือก **D1 database** → Variable name พิมพ์ว่า `DB` (ตัวพิมพ์ใหญ่ทั้งหมด สำคัญมาก ต้องตรงเป๊ะ) → เลือกฐานข้อมูล `kaag208-db` ที่สร้างไว้ → **Add binding**

3. **ตั้งรหัสผ่านอาจารย์**
   Worker เดิม `kaag208-proxy` → แท็บ **Settings** → **Variables and Secrets** → **Add** → Type เลือก **Secret** → Name พิมพ์ว่า `INSTRUCTOR_PASSWORD` (ตัวพิมพ์ใหญ่ทั้งหมด ต้องตรงเป๊ะ) → Value ใส่รหัสผ่านของคุณเอง (จำง่ายแต่คนอื่นเดาไม่ได้) → **Save**

4. **วางโค้ดใหม่**
   Worker เดิม `kaag208-proxy` → **Edit code** → เลือกโค้ดเดิมทั้งหมดแล้วลบ → คัดลอกเนื้อหาทั้งหมดจากไฟล์ `cloudflare_worker_proxy.js` ที่แนบมาพร้อมกันนี้ไปวางแทน → **Deploy**

5. **ทดสอบ**
   เปิดไฟล์ `KAAG208_บทเรียนออนไลน์.html` (ดับเบิลคลิก หรือเปิดผ่านเบราว์เซอร์) — ที่หัวเว็บบทเรียนจะขึ้นข้อความ
   `🟢 เชื่อมต่อฐานข้อมูลแล้ว` แปลว่าเชื่อมต่อสำเร็จ
   ถ้าขึ้น `🔴 เชื่อมต่อฐานข้อมูลไม่สำเร็จ` ให้ตรวจสอบว่าชื่อ binding เป็น `DB` และชื่อ secret เป็น `INSTRUCTOR_PASSWORD` ตรงตามข้อ 2-3 เป๊ะหรือไม่ (พิมพ์ผิดแม้ตัวเดียวก็ใช้ไม่ได้)
   สำหรับ Dashboard อาจารย์ เปิดไฟล์ `KAAG208_instructor_dashboard.html` แล้วกรอกรหัสผ่านอาจารย์ที่ตั้งไว้ในขั้นตอนที่ 3 — ถ้าขึ้นภาพรวมนักศึกษาแปลว่าเชื่อมต่อสำเร็จ

## การดูข้อมูลที่บันทึกไว้

**วิธีที่ 1 (แนะนำ):** เปิดไฟล์ `KAAG208_instructor_dashboard.html` → กรอกรหัสผ่านอาจารย์ → เลื่อนลงมาที่หัวข้อ **"🗂️ ข้อมูลดิบ"** → เลือกตารางที่ต้องการดู (การเช็คชื่อ/ทำครบ, แบบทดสอบก่อน-หลังเรียน, แบบฝึกหัดรายข้อ) → ค้นหาข้อมูลได้ หรือกด **"⬇️ ดาวน์โหลดเป็น CSV"** เพื่อเปิดใน Excel/Google Sheets ต่อได้

**วิธีที่ 2 (สำหรับผู้ที่ถนัด SQL):** เข้า Cloudflare dashboard → **Storage & Databases** → กดฐานข้อมูล `kaag208-db` → แท็บ **Console** → พิมพ์คำสั่ง SQL เอง เช่น `SELECT * FROM log ORDER BY id DESC LIMIT 50;`

## Learning Analytics แบบพยากรณ์ (สำหรับ นักศึกษา และ อาจารย์)

ระบบคำนวณ **ดัชนีความพร้อม (risk score, 0–100)** ของนักศึกษาแต่ละคนจากพฤติกรรมการทำแบบฝึกหัดจริง 3 ตัวแปร ได้แก่ ความแม่นยำในการตอบ "ครั้งแรก" ต่อข้อ, จำนวนครั้งที่ลองซ้ำเฉลี่ยต่อข้อ, และสัดส่วนครั้งเรียน (1–3) ที่ทำครบแล้ว — เป็นแนวทางเดียวกับระบบ early-alert ที่ใช้ในมหาวิทยาลัยต่างประเทศ (เช่น Purdue Course Signals) พร้อมคำแนะนำหัวข้อที่ควรทบทวนเป็นรายบุคคล (คำนวณจากหัวข้อที่ตอบผิดในความพยายามครั้งแรกบ่อยที่สุด)

- **นักศึกษา** ดูผลวิเคราะห์ของตัวเองได้ที่แท็บ "📊 ผลวิเคราะห์การเรียนของฉัน" ในเว็บบทเรียน (กรอกรหัสนักศึกษาของตัวเอง)
- **อาจารย์** ดูภาพรวมทั้งชั้นได้ที่ไฟล์ `KAAG208_instructor_dashboard.html` (ต้องกรอกรหัสผ่านอาจารย์) — เห็นอันดับความพร้อมของนักศึกษาทุกคน, หัวข้อที่ทั้งชั้นตอบผิดบ่อยที่สุด, และดูข้อมูลดิบ/ดาวน์โหลดเป็น CSV ได้

สูตรคำนวณและน้ำหนักที่ใช้ดูได้ในคอมเมนต์ของฟังก์ชัน `computeInsightsFromRows` ในไฟล์ `cloudflare_worker_proxy.js` — ปรับน้ำหนักหรือเกณฑ์ได้ตามดุลยพินิจของผู้สอน

## หมายเหตุสำคัญ

- **ถ้าแก้โค้ด `cloudflare_worker_proxy.js` ในภายหลัง** แค่เปิด Worker `kaag208-proxy` → **Edit code** → วางโค้ดใหม่ → **Deploy** ง่ายกว่า Apps Script เดิมมาก (ไม่ต้องจัดการ "deployment version" อะไรทั้งสิ้น ลิงก์เดิมใช้ได้ทันที)
- ข้อมูลรหัสนักศึกษา อีเมล และผลการทำแบบฝึกหัดถือเป็นข้อมูลส่วนบุคคล **อย่าแชร์รหัสผ่านอาจารย์หรือลิงก์ไฟล์ Dashboard ให้บุคคลอื่นนอกเหนือจากผู้สอน** ระบบนี้ออกแบบมาเพื่อการตรวจสอบการเข้าเรียน/ความคืบหน้าในการทบทวนเท่านั้น ไม่ใช่ระบบสอบที่มีการป้องกันการโกงขั้นสูง
- ถ้ายังไม่ได้ผูกฐานข้อมูล D1 หรือยังไม่ได้ตั้ง secret (ขั้นตอนที่ 2-3) เว็บบทเรียนจะยังใช้งานได้ปกติในโหมดออฟไลน์ แต่จะเก็บข้อมูลเช็คชื่อ/ความคืบหน้าไว้ในเบราว์เซอร์เครื่องนั้นชั่วคราวเท่านั้น ไม่สามารถตรวจสอบข้ามอุปกรณ์ได้ แท็บ "แบบทดสอบจริง" และ "ผลวิเคราะห์การเรียนของฉัน" จะใช้งานไม่ได้จนกว่าจะเชื่อมต่อฐานข้อมูล และหน้า Dashboard อาจารย์จะใช้งานไม่ได้เลยจนกว่าจะตั้งค่าเสร็จเช่นกัน
- ไฟล์ `KAAG208_AppsScript_Backend_Code.gs` (Google Apps Script เดิม) **เลิกใช้งานแล้ว** เก็บไว้ในโปรเจกต์แค่เป็นข้อมูลอ้างอิงเท่านั้น ไม่มีผลต่อระบบที่ใช้งานจริงอีกต่อไป
